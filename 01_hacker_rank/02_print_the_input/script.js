// Use what you learned in the previous challenge to complete the processData function by printing the  parameter to the console.

let input = "How many chickens does it take to cross the road?";

function processData(input) {
    console.log(input);
}

// Calling the function here to get an output 
processData(input);